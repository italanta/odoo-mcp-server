{
  "manifest_version": "0.4",
  "name": "odoo-mcp-server",
  "display_name": "Odoo MCP Server",
  "version": "2.0.0",
  "description": "Give MCP clients safe, profile-bound Odoo access with out-of-band credential onboarding and exact write approvals.",
  "icon": "assets/logo.png",
  "author": {
    "name": "Elewa"
  },
  "server": {
    "type": "uv",
    "entry_point": "src/mcp/odoo/server.py",
    "mcp_config": {
      "command": "uv",
      "args": [
        "run",
        "--directory",
        "${__dirname}",
        "src/mcp/odoo/server.py"
      ],
      "env": {
        "ODOO_MCP_ENABLE_WRITES": "${user_config.enable_writes}",
        "ODOO_MCP_ENABLE_SELF_UPDATE": "${user_config.enable_self_update}"
      }
    }
  },
  "compatibility": {
    "claude_desktop": ">=0.10.0",
    "platforms": [
      "darwin",
      "win32"
    ],
    "runtimes": {
      "python": ">=3.11"
    }
  },
  "user_config": {
    "enable_writes": {
      "type": "boolean",
      "title": "Enable writes to Odoo",
      "description": "Allow exact, principal-bound approved create/update operations to execute against Odoo. When off, write tools fail closed. Restart the extension after changing this.",
      "required": false,
      "default": false
    },
    "enable_self_update": {
      "type": "boolean",
      "title": "Enable self-update",
      "description": "Allow the server to update its own code from GitHub when you approve an update in conversation. When off, update checks still work but applying an update is blocked. Restart the extension after changing this.",
      "required": false,
      "default": false
    }
  },
  "tools_generated": true,
  "keywords": [
    "odoo",
    "mcp",
    "crm",
    "projects"
  ],
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/elewa-git/odoo-mcp-server"
  }
}
